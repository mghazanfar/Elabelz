import React from "react";

const Footer = props => (
  <div
    style={{
      height: 250,
      display: "flex",
      flex: 1,
      backgroundImage:
        "linear-gradient(204deg, rgb(20, 165, 222), rgb(0, 0, 0))"
    }}
  >
    I am footer
  </div>
);

export default Footer;
